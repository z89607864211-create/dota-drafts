{
  "name": "dota-drafts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd backend && npm run dev",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm start"
  }
}
